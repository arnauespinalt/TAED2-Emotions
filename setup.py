from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='We are the team that develops a model to classify some text input into a variety of emotions.',
    author='EmotionTeam',
    license='MIT',
)
